package org.Launchcode;

public class OrderService {
    public Double calculatePrice(Basket basket) {
//        return basket.getProductsQuantity().entrySet().stream()
//                .map(e -> e.getKey().getPrice() * e.getValue())
//                .reduce(0.0, Double::sum);
        return null;
    }
}
